# Minimal set for `make demo` (dashboard with the in-process pipeline core)
# and for running the unit tests. No Kafka, Spark, or database clients.
numpy>=1.26.0
pandas>=2.1.0
plotly>=5.18.0
streamlit>=1.37.0
click>=8.1.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
pytest>=7.4.0
